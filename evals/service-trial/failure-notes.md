# 服务试用开发中发现的问题

以下是首次运行的实际失败，不把它们改写为通过。

- 服务端初次11/12：测试误写不存在的inventory.available，实际结构为capacity/occupied；改为占用全部容量，仍验证比较无结果和旧报价确认拒绝。
- 浏览器初次0/3：模拟客服入口沿用普通登录请求，多传conversation_id，被严格schema拒绝；修正前端请求。另一失败是NSA-A选择器匹配了CANCEL/AGENT同后缀，改用准确可访问名称。
- 验收脚本新增直调报价后TypeScript发现动作类型被推断为string，构建失败；补充字面量类型。该失败期间页面仍是旧构建，重复入口失败未计为通过。
- 浏览器随后2/3：成功标题包含✓图标，测试原本要求整段文本完全相等；改为核对明确业务文案，不改变成功条件。
- 最终源码冻结后：全套129/129业务、16/16浏览器、36知识及10检索通过；全新解压安装目录再次通过129业务及全部知识测试。真实模型核心10、自由表达8、服务查询3分别完成，未使用替身填补真实结果。
