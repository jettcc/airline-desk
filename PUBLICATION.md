# 公开发布说明

2026-09-20，仓库：`jettcc/airline-desk`。

本版本来自已交付的本地项目；保留业务代码、测试、计划、源政策、知识库索引、历史验证证据与脱敏后的可见会话。公开发布只调整入口文档、忽略规则和授权演示配置，不改变业务实现。

## 演示配置

仓库所有者明确授权将演示 Key 加入公开仓库，并告知已将 Right Codes 后台额度上限调整为 **3 美元**。该 Key 仅出现在 `.env.rightcodes.local` 中，模型为 `gpt-5.6-sol`，启动程序会自动加载。公开仓库的访问者均可读取并使用该演示 Key。

`AIRLINE_MODEL_BUDGET_USD=3` 是所有者提供的额度说明，不是本地扣费拦截。供应商后台限额与剩余额度未独立核验；本次更新只验证配置加载和发布，不调用收费模型。失效或额度耗尽时，可在本地替换配置，但不要将私用凭据提交回仓库。

除此授权演示 Key 外，仍不包含 GitHub 凭据、原始 Codex 日志、实际用户数据库或私下交付 ZIP。`.env.example` 中的 Key 保持为空。

历史 PDF、验证日志与会话保留原样，其中的 5 美元上限描述之前的交付状态。历史文件指纹对应当时的快照，不应当作当前仓库的逐文件清单。

一键启动脚本支持 macOS/Linux。既有验证涵盖本地模拟业务与测试中列明的边界，不代表真实航司、真实退款或生产运营能力。公开可见性不等于授予额外的软件或源文档许可。
